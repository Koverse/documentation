parameters:
  - $ref: "../api.yaml#/parameters/sinkTypeId"

get:
  summary: Get
  operationId: findSinkTypeById
  tags: ["Sink Types"]
  responses:
    200:
      description: Returns the sink type data
      schema:
        $ref: "../api.yaml#/definitions/sinkTypeDescription"
      # headers:
      #   last-modified:
      #     type: string
      #     description: The date/time that the sink type was last modified
