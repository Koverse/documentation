get:
  deprecated: true
  summary: List
  operationId: dataSetPermissions
  tags: ["Data Set Permissions"]
  description: |
            List available data set permissions
  responses:
    200:
      description: |
            Returns available data set permissions that can be applied, such as the permission to read, write, etc.
      schema:
        type: array
        items:
          type: string
      # example:
      #   - readData
      #   - manageDataCollectionConfiguration
      #   - manageDataCollectionPermissions
      #   - writeDeleteData
      #   - bulkDownloadData
      #   - viewAuditEvents
