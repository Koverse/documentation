get:
  deprecated: true
  summary: List types
  operationId: findNormalizationTypes
  tags: ["Normalizations"]
  description: |
            List all available Normalization types.
  responses:
    200:
      description: Returns the matching normalization types
      schema:
        type: array
        items:
          $ref: "../api.yaml#/definitions/normalizationType"
