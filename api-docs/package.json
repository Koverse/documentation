{
  "name": "koverse-api-docs",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "swagger": "swagger-cli bundle api.yaml -t yaml -o api-compiled.yaml && swagger-cli validate -d api-compiled.yaml",
    "build": "npm run deref && spectacle -a app api-compiled.yaml",
    "start": "npm run deref && spectacle -a app -d api-compiled.yaml",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-dereference-cli": "^0.1.2",
    "spectacle-docs": "^0.9.11"
  }
}
