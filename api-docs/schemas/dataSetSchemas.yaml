type: object
description: >
  Array of metadata of the schema of a dataset.
required:
  - type
  - fieldTypeCounts
properties:
  type:
    type: string
    description:
    example: SCALAR
  fieldTypeCounts:
    type: array
    description:
  fieldValueType:
    type: string
    description:
    example: DATE
