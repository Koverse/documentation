get:
  summary: Returns search results
  operationId: searchResults
  tags: ["Search"]
  responses:
    default:
      description: Returns the search results
      schema:
        type: object
        properties:
          dataSets:
            type: array
          labels:
            type: array
