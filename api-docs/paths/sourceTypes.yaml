get:
  summary: Returns all source types
  operationId: findSourceTypes
  tags: ["Sources"]
  responses:
    default:
      description: Returns the matching source types
      schema:
        type: array
        items:
          $ref: "../api.yaml#/definitions/sourceType"
