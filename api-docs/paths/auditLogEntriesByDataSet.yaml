parameters:
  - collectionId:
    type: string
  - startIndex:
    type: integer
    format: int32
  - limit:
    type: integer
    format: int32
  - query:
    type: string
gett:
  summary: Audit log entries by data set
  operationId: auditLogEntriesByDataSet
  tags: ["Audit Logs"]
  responses:
    default:
      description: Returns Audit Log Entries
      schema:
        type: array
        items:
          $ref: "../api.yaml#/definitions/auditLogEntry"