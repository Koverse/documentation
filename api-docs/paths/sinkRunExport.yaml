parameters:
  - $ref: "../api.yaml#/parameters/sinkId"

get:
  summary: Run export by sink id
  operationId: exportBySinkId
  tags: ["Sinks", "Exports"]
  description: Run an export by the specified sink id.
  responses:
    default:
      description: Returns the export job data
      code: 200
      schema:
        $ref: "../api.yaml#/definitions/job"
