get:
  description: Returns the available data set permissions
  summary: Get data set permissions
  operationId: dataSetPermissions
  tags: ["DataSets", "Permissions"]
  responses:
    default:
      description: Returns avilable data set permissions.
      code: 200
      schema:
        type: array
        items:
          permission:
            type: string
      example:
        - readData
        - manageDataCollectionConfiguration
        - manageDataCollectionPermissions
        - writeDeleteData
        - bulkDownloadData
        - viewAuditEvents
