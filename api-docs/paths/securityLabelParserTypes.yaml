get:
  summary: Returns all security label parser types
  operationId: findSecurityLabelParserTypes
  tags: ["Security Label Parsers"]
  responses:
    default:
      description: Returns the matching security label parser types
      schema:
        type: array
        items:
          $ref: "../api.yaml#/definitions/securityLabelParserType"
