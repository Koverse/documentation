post:
  summary: Search audit log entries
  operationId: searchAuditLogEntries
  tags: ["Audit Logs"]
  parameters:
    - name: query
      in: formData
      description: "The Addon data to upload"
      required: true
      type: string
  responses:
    default:
      description: Returns all Audit Log Entries
      schema:
        type: array
        items:
          $ref: "../api.yaml#/definitions/auditLogEntry"