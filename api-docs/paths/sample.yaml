parameters:
  - $ref: "../api.yaml#/parameters/importFlowId"
get:
  summary: Returns sample data from an import flow
  operationId: findSample
  tags: ["Import Flows", "Sample"]
  responses:
    default:
      description: Returns the sample for an import flow
      schema:
        type: array
        items:
          $ref: "../api.yaml#/definitions/sample"
