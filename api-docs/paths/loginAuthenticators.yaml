get:
  summary: Get all login authenticators
  operationId: getLoginAuthenticators
  tags: ["System"]
  responses:
    default:
      description: Returns the user data
      schema:
        type: array
        items: object
