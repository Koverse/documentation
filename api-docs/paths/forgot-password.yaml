post:
  summary: Send a reset password email to the specified email address
  operationId: forgotPassword
  tags: ["System"]
  parameters:
    - name: forgotPassword
      in: body
      description: forgot password object
      required: true
      schema:
        $ref: '../api.yaml#/definitions/forgotPassword'

  responses:
    default:
      description: Returns the success data
      schema:
        $ref: "../api.yaml#/definitions/success"
