get:
  summary: Returns recent searches
  operationId: searchRecent
  tags: ["Search"]
  responses:
    default:
      description: Returns the recent searches
      schema:
        type: array
        items:
          $ref: "../api.yaml#/definitions/searchRecent"
