post:
  summary: Updates the logged in users password.
  operationId: updatePassword
  tags: ["NOT IMPLEMENTED YET", "System"]
  parameters:
    - name: updatePassword
      in: body
      description: update password object
      required: true
      schema:
        $ref: '../api.yaml#/definitions/updatePassword'

  responses:
    default:
      description: Returns the success data
      schema:
        $ref: "../api.yaml#/definitions/success"
