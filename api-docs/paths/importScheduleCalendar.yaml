parameters:
  start:
    name: start
    in: query
    description: filters events after the start param
    type: string
  end:
    name: end
    in: query
    description: filters events before the end param
    type: string

get:
  summary: Returns the matching calendar events
  operationId: findCalendarEvents
  tags: ["NOT IMPLEMENTED YET", "Import Schedules"]
  responses:
    default:
      description: Returns the event data
      schema:
        $ref: "../api.yaml#/definitions/calendarEvent"
      headers:
        last-modified:
          type: string
          description: The date/time that the label was last modified
