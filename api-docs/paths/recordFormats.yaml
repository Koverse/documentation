get:
  description: Returns all record formats
  operationId: findRecordFormats
  responses:
    default:
      description: Returns the available record formats
      schema:
        type: array
        items:
          $ref: "../api.yaml#/definitions/recordFormat"
