parameters:
  - $ref: "../api.yaml#/parameters/jobId"

get:
  summary: Unblocks a job by job id.
  operationId: shutdownImportJob
  tags: ["Jobs"]
  responses:
    default:
      description: '200 OK'
